[pytest]
# Keep collection rooted in tests so the legacy application package is not imported.
testpaths = .
# Let focused tests import project modules from the current checkout explicitly.
pythonpath = ..
