# E24 A/B/C 单因素离线对照（2026-09-17）

用户明确要求执行对话 `01a0ad47-5cb1-7fe0-a126-7a8941af456d` 最后定义的A/B/C三组。本轮复用已验收缓存，不重新采集，不启动D，也不接回完整导航。

## 固定协议

- 原基线：`training_20260917/formal_seed2`，只比较250—6000共24个保存点。原最佳4750点净+24。
- A：已选定4750/4000两个头，倍率固定 `{0,0.5,0.75,1,1.25,1.5}`，实际修正 `clamp(g*delta,-1,1)`，共12组合。0为不改分基线；不增加网格。
- B：网络、数据顺序及全部损失权重不变，仅将完整候选上的分类/排序监督扩大到教师有效、基础和教师均MOVE且至少一个future有效的行。方向监督、少改动约束和残差正则保持原样。本轮不混入可纠正性加权。
- C：损失保持原基线，节点/文字/几何/基础分数和候选交互使用全部真实Top-5候选；没有future时跳过整个future读取分支，仅原future有效槽可输出可执行修正。真实候选由缓存索引>=0判断，不依赖教师信息。
- B/C随机初始化、seed2、batch32、AdamW lr2e-5/weight decay0.01、梯度裁剪10、BF16、6000更新、每250保存、每20记录。学习率固定，样本顺序与原基线相同；本轮固定6000不自动延长。
- B/C均以g=1评价完整dev；选点依次按净改善降序、改坏升序、分类损失升序、步数升序。报告全曲线、相邻点、分场景和教师future分组，不只挑最好点。
- 主要分母13389移动决策，完整缓存1839条路线/15703行。val_unseen已多轮参与选点，是开发集，不能当独立测试集或导航SR/SPL。

## 机器与产物

继续使用三机 `ETP-R1-stage2-e24` 独立工作区和 `feature/stage2-e24-offline`。训练机 `etpnav_unified`，GPU0/1分别执行B/C；测评机 `gwl-etpr1-rae` / `etpr1_rae` 串行评价，检查GPU与受保护任务后才启动。训练缓存留在训练机，dev缓存留在测评机；仅头模型和结果通过专线传输，源码只经Git。

新实验相对根 `data/logs/stage2_e24/abc_20260917`。A报告在测评机 `A_calibration.json`；B/C训练分别为 `B_seed2_retry1`、`C_seed2`，各自 `offline_eval` 保留全量结果。精确命令、版本、父子PID及退出码见每次 `jobs/*/worker.json` 和 `run.log`。

## 验收及状态

A已在原代码dc8cfaf完整执行，退出0，g=1逐项重现原105/81与120/103。4750×1.5最高净+27（132改对/105改坏），仅比原最佳多3条；缩小倍率均未超过原最佳，不支持“只需更谨慎就能明显改善”。

B/C正式训练前的相关回归和真实20步连续训练与10+10跨进程恢复检查均通过。最终结果见下方。


### 正式启动与预检

源码e90d700，训练机55项、测评机22项相关回归均退出0。B/C真实20步连续训练分别与10步中断后新进程恢复至20的模型、优化器、采样器、全部随机状态逐位相同，`preflight_{B,C}/pipeline.json` passed。沿用上一轮缓存/特征/冻结基座验收，离线训练只构建E24，不加载导航与世界模型，不重复启动无关导航长流程。

C于15:33启动，B于15:34在`B_seed2_retry1`启动。B首次尝试因评价监督过早创建输出子目录，被训练入口的“新实验目录必须为空”保护拒绝；未发生任何训练更新。旧`jobs/B`、空实验和监督日志保留，停止了本轮自己的旧空监督进程，改用新目录，确认训练初始化后才启动`queue_retry1`。这是启动次序修正，不是重试选种子或改计算配置。C运行不受影响。

运行命令由`{B_retry1,C}_launch.json`、`queue_retry1_launch.json`保存；只传头和结果，开发特征仍留在测评机。两组最终均固定6000更新，全部24点评价已完成。


### 训练完成和B完整结果

B/C均于训练机15:54:32完成6000更新，两个worker退出0，单组含启动开销约20—21分钟。计算源码保持e90d700；报告工具034121f仅新增汇总和文档，未修改训练计算。

B全部24点已完成。最佳5000步为59改对/47改坏/净+12，准确率59.9373%；相对基座的配对95%提升区间为[-0.0672,+0.2493]个百分点，跨0。4500/4750/5000/5250/5500/5750/6000步净值依次为+7/+6/+12/+1/+9/-4/0。与原最佳相比，正确候选缺future组从0改对/55改坏变为4改对/16改坏（净-12），但有future组从105改对/26改坏变为55改对/31改坏（净+24），整体未胜过原净+24。此比较使用各自按预定规则选中的最佳点，不代表单个分组的独立因果估计。

### 最终对比（9月17日完成实验，9月20日完成收尾审计）

训练机监督记录C于2026-09-17 16:21:34完成最后一个评价；队列和两组评价监督均completed/exit_code=0。所有实验均复用完整开发集，主分母13389，停止动作始终保持不变。

| 方案 | 选中步数 / 倍率 | 改对 | 改坏 | 净改善 | 离线移动准确率 |
| --- | --- | ---: | ---: | ---: | ---: |
| 原方案前6000步 | 4750 / 1 | 105 | 81 | +24 | 60.0269% |
| A：倍率调整 | 4750 / 1.5 | 132 | 105 | +27 | 60.0493% |
| B：扩大监督覆盖 | 5000 / 1 | 59 | 47 | +12 | 59.9373% |
| C：真实候选参与比较 | 5250 / 1 | 130 | 111 | +19 | 59.9895% |

C选中点：有future组130改对/36改坏/净+94，无future组0改对/75改坏/净-75；相比原最佳的+79/-55，有信息时收益提高，但缺信息时误改也更多，总收益仍低于原方案。C在4750/5000/5250/5500/5750/6000步净值依次为+17/+13/+19/+11/-15/-3。其配对95%提升区间为[-0.1193,+0.4230]个百分点，跨0。

前6000步的24个保存点中，原方案22点净收益为正，B为9点，C为21点。原方案、B、C选中点均有7/11场景净收益为正，去掉最大收益场景后分别仍有+13/+8/+12；收益并非只由单个场景贡献，但幅度仍小。

本轮结论：A只有额外3条正确决策（约0.0224个百分点），B/C未超过原最佳，尚无证据支持将其中一组视为明显更好的方案。A/B/C与冻结一阶段相比的配对区间均跨0；这些区间没有校正开发集选点偏差，也不能直接当作三组之间的显著性检验。本轮只有随机种子2，没有完整导航SR/SPL验证，不据此替换在线导航模型。

### 最终审计与产物

`report_stage2_abc.py --verify-checkpoints`在训练机`etpnav_unified`中退出0：A精确覆盖12组合且倍率1复现旧结果；B/C各24点完整覆盖；全部模型及结果SHA链一致；初始参数指纹相同，原方案/B/C的300个训练日志采样位置逐项一致。B/C最终头模型与完整恢复状态权重逐项相同，分别检查500个模型/优化器张量全部有限，优化器仅包含E24，最终采样器均为epoch3/block94/offset17。

完整汇总命令（在训练机独立工作区）：

```bash
bash scripts/rgb_only_optimization_runtime.sh server scripts/report_stage2_abc.py \
  --baseline-dir data/logs/stage2_e24/training_20260917/formal_seed2 \
  --a-report data/logs/stage2_e24/abc_20260917/A_calibration.json \
  --b-dir data/logs/stage2_e24/abc_20260917/B_seed2_retry1 \
  --c-dir data/logs/stage2_e24/abc_20260917/C_seed2 \
  --output data/logs/stage2_e24/abc_20260917/final_report --verify-checkpoints
```

退出0，关键输出`status=complete`。日志：实验根`report_audit.log`。汇总工具源码034121f；实验计算源码e90d700。

训练机实验根`final_report/`保存`summary.json`、`report.md`、`curves.svg`、`curves.png`。仅这四个小型报告已复制至笔记本本独立工作区`data/logs/stage2_abc_report_20260917/`；模型、训练状态、缓存和训练日志仍在原机器。

选中头SHA：B `eddafa65e2c01faed9b344e6c5e5390fa19e884e20b8337bff1a8b06a8916253`；C `2b53d81db5fe588c249edf7be28e9cd65c2e60f1d649874e7ad39d9de07aa49c`。A继续使用原4750步头，倍率作为后处理配置，不伪装成新训练权重。
