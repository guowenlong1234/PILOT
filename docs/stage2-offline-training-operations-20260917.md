# E24 离线训练与开发集检查

## 本轮范围与固定数据

用户授权继续原对话最后回复的第1、2、3步：实现离线训练，验证短训练与跨进程恢复，完成正式训练并检查离线效果。本轮不接回在线导航，也不启动完整导航评测。

继续使用三机已有 `ETP-R1-stage2-e24` 独立工作区和 `feature/stage2-e24-offline` 分支。主工作区和论文修改不参与本轮。

- 训练机使用 `etpnav_unified`，读取 `data/logs/stage2_e24/formal_train_part{0,1}/episodes` 两份完整缓存：10819条路线，82674行，其中58556行有有效训练信号。
- 测评机在 `gwl-etpr1-rae` / `etpr1_rae` 内读取 `data/logs/stage2_e24/formal_dev/episodes`：1839条路线，15703行。
- 开发集沿用 val_unseen，曾参与一阶段选点，不能称为独立测试集。其基础与教师均移动且教师有效的13389行作为主要离线决策分母；无有效future的行保持原选择。
- 固定一阶段6400步及预测future缓存，身份和完整验收证据见 `stage2-offline-collection-operations-20260916.md`。

## 预先固定的训练和选点规则

E24从随机初始化开始，最后输出层为零；只构建和更新E24，不加载导航、融合、世界模型或路点预测器。批量32，AdamW学习率2e-5、weight decay 0.01、梯度裁剪10、seed 2；固定学习率，最多10遍有效样本或20000更新，先到者停止。损失使用计划步骤8的显式权重。

每250步保存头模型并评价完整开发集，固定修正倍率g=1。按净修正数最高、破坏数最低、分类损失最低、更新步数最早依次排序。首轮不在全部模型上搜索倍率。完整训练状态保留最近3份及每2000步长期点，用于恢复；头模型保留全部评价点。

正式训练前必须通过真实20步连续训练与10步中断后新进程恢复至20步的比较。记录样本顺序、损失、模型、优化器和随机状态的一致性，以及梯度有限、E24参数更新和优化器参数范围。

## 运行环境

2026-09-17现场导入检查退出0：训练机Python3.10.14，测评机Python3.11.15；两机PyTorch2.2.2+cu121、CUDA12.1、Transformers4.49.0、Habitat/Habitat-Sim0.3.3。训练机两张A6000空闲；测评机现场GPU为RTX3090 24GB且空闲，受保护的ETPNav容器处于停止状态。未修改任何环境或驱动。

训练机继续使用项目自有 `.runtime/nvidia-580.173.02/root/usr/lib/x86_64-linux-gnu` 用户态库。后台任务通过薄包装 `scripts/stage2_offline_worker.py` 使用既有GPU锁、资源检查和对应机器运行时；每次尝试单独写 `worker.json` 和 `run.log`，保存实际命令、版本、源码、父子PID和退出码。

## 执行结果

### 真实验收已通过

源码 `506023d`。训练机39项、测评机30项相关测试退出0（有重叠，不相加称独立测试数）；日志为两机 `data/logs/stage2_e24/training_20260917/tests_{server,eval}_v2.log`。首次BF16冒烟发现旧E24最终索引写入的dtype不一致，已显式把最终低精度分数转到目标缓存dtype；FP32计算不变，失败记录保留于 `preflight_bf16` 与测评机 `jobs/zero`。

训练机 `preflight_bf16_v2/pipeline.json` 为passed，退出0。连续20更新与10更新后新进程恢复至20的125个模型张量、optimizer、sampler、Python/NumPy/Torch/CUDA随机状态、scaler（BF16无scaler）、scheduler（固定学习率为null）及计算合同逐位相同。两条运行的每步损失、梯度数值及样本位置也完全相同；比较时仅忽略各自实验目录、吞吐和显存统计。证据为 `resume_comparison.json` 与两份 `train.jsonl`。

`parameter_audit.json` 核验优化器仅含60854785个E24参数；初末SHA不同，前3次更新内各主要模块均出现非零梯度。冻结的一阶段及世界模型等未被实例化，不可能进入本离线优化器；既有数据和权重文件只读。

测评机 `zero_v2.json` 完整覆盖1839路线、15703行，主要分母13389；基础正确8013行，准确率59.847636%。零初始化头的全体动作变化为0，15703行STOP状态全部一致。全量SHA复验及真实BF16前向通过，评价用时约44.58秒；`jobs/zero_v2/worker.json` 退出0。

### 正式训练已启动

北京时间2026-09-17 11:04:42，训练机GPU0启动 `formal_seed2`，父PID532937、训练子PID532945；PID仅作为历史记录，重连须核对命令。GPU1未占用。约11:04:59启动离线测评监督PID533006。源码固定为 `506023d`，BF16已由真实恢复验收锁定。

精确命令分别见 `training_20260917/formal_launch.json`、`watch_launch.json` 和 `jobs/formal/worker.json`。正式参数使用脚本默认值（batch32、lr2e-5、seed2、10epoch、20000上限、每250保存、每20记录），额外显式 `--precision bf16`。58556条有效行保留每遍末小批，实际目标18300更新；不混入预检数据。

- 训练机真实产物根：`/mnt/data2tb/ETP-R1_data/experiments/stage2_e24_persistent6400_v1_20260916/training_20260917/formal_seed2/`。
- 测评机评估根：`/home/a6000/gwl/ETP-R1-stage2-e24/data/logs/stage2_e24/training_20260917/formal_seed2/`。
- 训练状态 `status.json`，训练指标 `train.jsonl`，参数审计 `parameter_audit.json`。
- 完整离线曲线和选点位于训练机 `offline_eval/curve.json`、`selection.json`；每点评价的全场景/逐路线结果、分层及配对区间保留为 `metrics_step_*.json`。测评机保存对应模型和每点退出记录。
- 每个模型经训练机—测评机专线传输，`.partial`完成SHA核验后原子发布；仅传头模型和小型结果，不迁移训练/开发特征。源码始终通过Git同步。

启动后已核对父子进程存活、训练指标增长、GPU0有计算负载和独立实验目录。正式训练及选点完成情况在下方补充。

### 正式训练完成，最终状态审计通过

北京时间11:53:24训练正常结束，退出0，耗时约48分41秒；完成10遍、18300更新，74份头模型齐全。训练机两张GPU均恢复0%计算利用率，仅保留45/16MiB显示占用。

`completion_audit.json`：最终完整状态与头模型的125个张量逐项一致且有限，optimizer状态所有张量有限；epoch=10、global_step=18300、scheduler/scaler均为null；915个每20步指标窗口连续无缺失且数值有限。开发集逐点评价继续在测评机完成，最终结论见下方收尾汇总。

精确恢复合同包含源码提交、原实验目录、全部训练配置和数据manifest，重放本轮时使用记录的 `506023d`；后续只增加报告或文档的提交也不自动豁免源码检查。更换数据、预算或初始化方式应另立实验，不能冒充本轮精确resume。

## 最终离线结果

北京时间12:16:12，74个保存点的完整开发集评价全部完成，监督退出0；`offline_eval/watcher.json` 与 `selection.json` 均为completed。每点完整覆盖1839条路线、15703行记录，主分母13389，STOP状态全部不变。训练与所有模型的开发评价共用时约71分30秒；单次训练约48分41秒。

| 模型 | 改对 | 改坏 | 净增加正确 | 离线移动决策准确率 |
| --- | ---: | ---: | ---: | ---: |
| 一阶段基础 | 0 | 0 | 0 | 59.8476% |
| 首选：4750步 | 105 | 81 | +24 | 60.0269% |
| 备选：4000步 | 120 | 103 | +17 | 59.9746% |
| 最后：18300步 | 190 | 230 | -40 | 59.5489% |

首选提升仅0.1793个百分点；按路线配对重采样的95%区间为[-0.0452,+0.4144]个百分点，包含0，且没有校正从74点选最优产生的偏差。74点中仅27点净改善为正，最差13000步净少127条正确决策。后期训练损失继续下降而开发表现下降，有过拟合迹象；这轮不能认为已证明稳定收益。

首选模型：训练机产物根下 `head_step_004750.pt`，SHA256 `598986525cb3ac4696743b0480ac6733b918e3d4407d647c4cddb0a498ae286e`；测评机评估根下 `heads/head_step_004750.pt` 为同一文件。完整模型与优化器状态留在训练机，所有头模型留存；没有传回笔记本。没有校准倍率、平均权重或追加试验，也没有接回完整导航，因此这些数值不是导航成功率或SPL。

`scripts/report_stage2_offline.py <训练根>` 在训练和全部评价完成后核验完整点集、模型SHA、覆盖率、STOP与最终状态审计，再生成 `final_report/summary.json`、`report.md`、`curves.svg`、`curves.png`。只有这组小型报告可复制到笔记本 `data/logs/stage2_e24_report_20260917/`；运行产物不提交Git。
