# Writing Rationale Matrix

| Row ID | Manuscript Unit | Current/Planned Function | Motivation Link | Reference/SOTA Pattern Learned | Target Scene or Venue Norm | User Evidence or Citation Anchor | Planned Change | Final Text Check |
|---|---|---|---|---|---|---|---|---|
| F1 | 全文框架与论证主线 | 以一个可审查问题组织全文：未来表征怎样在不读取真实未来观测时帮助在线拓扑候选决策；RAE/DINOv2、RGB 融合、候选前视和 GRPO 都是回答该问题的层次，不作为平行首创堆叠。 | 用户确认方案 A，并选择逐级消融、未来来源与第一至第三步视野、动作翻转和误差/覆盖/开销分析。控制性动机要求把“当前证据不足的 ghost 选择”贯穿标题、摘要、方法图、主结果和讨论；若性能证据不足，贡献必须收缩为机制边界。 | SOTA 调研显示 ETPNav/ETP-R1 已覆盖图式规划与 GRPO，NavMorph 已覆盖 VLN-CE 世界模型，NavGRPO 已覆盖组相对强化学习。可迁移的优秀论文结构是“最近工作留下的具体接口缺口—设计回应—逐项证据”，而不是“首次组合多个热门模块”。 | 面向通用 IEEE 机器人与具身智能期刊，完整长文需要问题、方法、实验、讨论和局限闭环；目标期刊未定时不猜测具体模板，但维持 6 个一级章节、可复现公式、自包含图表和不跨协议比较的期刊规范。 | `confirmed_motivation.md`、`confirmed_contribution.md`、E05–E16、C1–C9、T1–T6、F1–F6；代码证据包括 `dino_cwp_future.py`、`joint_e24.py`、`frozen_grpo.py` 与 GRPO 测试。已核验 R2R SFT 基座 SR 63.7303%、SPL 55.6054%，其余性能只用具名结果键。 | 写作顺序固定为候选证据缺口、原生标记到 ghost 的受限接口、SFT 梯度契约、GRPO rollout 缓存语义、同协议主结果、逐级与多视野机制验证、边界讨论。删去未映射到贡献或证据的旧 ETP-R1 细节，并把内部项目管理信息移到清单。 | PASS：标题、摘要、引言贡献、方法总图、结果单元和结论均复述同一受限贡献；不出现首次世界模型/GRPO、全面领先或尚未完成实验的现在时结论。结果回填后需再次检查全文主张是否与清单和原始 JSON 一致。 |
| A1 | 标题、摘要与关键词 | 标题提供任务和机制，不提前承诺效果；摘要用问题—方法—系统证据—结果键—意义五个功能层，关键词覆盖连续导航、拓扑规划、世界模型、表征自动编码器与强化微调。 | 控制性动机要求读者首先理解“候选相关未来证据不足”，而不是先面对 NWM、CWP、E24 等缩写。摘要必须承诺实验 1/2/3/6 的验证范围，但不能把预计优于基线写成事实。 | IEEE Author Center 与期刊范例强调具体、描述性标题和自包含摘要。高质量摘要只报告能在结果中追溯的证据；NavMorph 的存在要求避免“首次为导航引入世界模型”的过宽措辞。 | 通用 IEEE 期刊摘要应为单段、无引用、无公式，标题避免 SOTA/novel/first 等自评词。中文母稿使用“摘要”标题；英文定稿再按选定期刊限制摘要长度与关键词数量。 | 标题由用户确认的方案 A 生成；摘要中的系统主张由严格权重加载、257 标记数值一致性、回退、梯度和恢复测试支持。`RESULT-SUMMARY` 由 `results_placeholder_manifest.md` 约束，不能手工猜数。 | 保留中文题名“面向连续环境视觉语言导航的受约束表征世界模型主动前视”。摘要用候选残差评分头替代未解释的 E24，只写 h=1 为现有路径；把 h=2/3 作为受控实验范围，并留唯一的具名结果总结键。 | PASS：标题无性能夸大；摘要强陈述均能映射到代码测试或结果表；不存在自由文本“待填”和虚构数字。投稿前只有在所有结果键有原始来源时才生成英文结果句。 |
| I1 | 引言问题、缺口与贡献 | 从 VLN-CE、在线拓扑图和候选不充分证据逐步收窄问题；随后用一段方法响应和三条贡献说明本文新增内容、训练契约和验证体系。 | 动机来自转角、遮挡与相似房间下多个 ghost 难以由当前观察区分。研究要解决的是未来线索如何合法进入候选动作空间，并限制错误预测的直接作用范围，而不是重新论证拓扑图或强化学习本身。 | R2R/VLN-CE 给任务定义，ETPNav/ETP-R1 给图式基线，NavMorph 给最近世界模型先例，NavGRPO 给最近强化学习先例。优秀引言会公平承认这些进展，再用一句可引用的差异锁定论文增量。 | 机器人期刊引言应同时回答这是什么问题、为什么重要、已有方法到哪一步、本文解决哪个未决挑战、证据如何验证。贡献列表需与结果单元一一对应，工程测试不单独包装成科学创新。 | 引用 `anderson2018r2r`、`krantz2020vlnce`、`an2025etpnav`、`ye2025etpr1`、`yao2025navmorph` 和 `li2026navgrpo`；贡献边界由 C1–C7 支持，C8/C9 仍依赖新实验。 | 明确区分旧 ETP-R1 与新增 RAE/DINOv2、RGB 世界模型融合、持久上下文、q0→q1、CLS 适配、候选残差评分和缓存残差 GRPO。贡献条目分别使用“提出方法”“建立契约”“构建验证体系”的不同句法。 | PASS：引言不把近邻工作写成失败，不声称宽泛首创；三条贡献均在 `results_validation.md` 至少有一个对应单元，且 h=2/3 未被写成已经运行。 |
| R1 | 相关工作与最近方法定位 | 按连续拓扑导航、视觉表征/世界模型、在线监督/强化三条问题轴综合文献；增加 NavMorph 功能表，让读者快速看到决策接口差异。 | 相关工作服务同一动机：为什么在线图仍需要候选未来证据，为什么原生 CLS+patch 有意义，以及为何 GRPO 必须有清楚的残差输入契约。它不能变成逐篇摘要或用引用数量制造新颖性。 | DINOv2 对应自监督视觉特征，RAE 对应高维生成潜空间，Dreamwalker/NavMorph 对应导航未来推演；DAgger、PPO、GRPO、ETP-R1 和 NavGRPO 分别说明在线数据、更新限制、组内优势和导航训练背景。 | 期刊相关工作必须正面处理最接近方法并陈述可验证差异。比较表应只使用公开论文支持的维度，不推测对方未报告的实现；引用采用真实 `\cite{key}` 与 BibTeX 数值机制。 | `citation_support_bank.md`、`citation_quality_audit.md` 和 `references.bib`；NavMorph 原文支持紧凑潜状态、两步想象、候选航点评分和上下文演化记忆。ETP-R1 论文支持图式 SFT/GRPO 背景。 | 加入未来表示、动作条件、决策对象、预测步数、干预位置、作用范围、测试时更新和主要成本八维对照。把“未设置等价约束”限定为与本文接口不同，不把它解释为对方缺少整体安全设计。 | PASS：所有引用键通过 LaTeX guard；比较表每个陈述有文献支撑；相关工作末尾把增量收束为逐 ghost 原生表征、受限干预和缓存残差语义。 |
| M1 | 方法定义、总体图与候选状态机 | 给出 $p_0/q_0$、$p_r/q_r$ 和视野 $h$ 的一一映射，说明 h=1 是候选到达后再向前一步；总图区分基础 ETP-R1、新增模块、已实现路径和计划扩展。 | q0/q1 与 h 的歧义会直接破坏 oracle、误差对齐和成本统计，因此时间索引是贡献成立的前提。候选级失败处理对应“错误预测作用范围受限”的核心动机。 | SOTA 参考模式要求可复现方法先定义决策对象、坐标条件、输入输出与失败规则，再列网络结构。NavMorph 论文的多步规划又要求本文清楚说明 h=2/3 不是既有成果。 | 机器人期刊的方法图需独立可读，标出冻结、可训练、缓存、掩码和无未来观测约束；公式、图、表和日志字段必须使用相同编号。 | 代码与测试证据包括 `etp_adapter.py` 的四帧快照、`dino_cwp_future.py` 的 q0/CWP/q1、`topk_query.py` 的稳定排序、E05–E08 的 257 标记一致性和失败计数；F1 的 Graphviz 源文件可编辑。 | 写作上定义 $q_0$ 为 ghost 到达位姿中间查询，$q_1$ 为前方一步；计划 h=2/3 沿用固定历史与累计位姿，只把端点送入评分头。单槽失败置零残差，不用其他候选补位；全行失败恢复基础分布。 | PASS：读者不看代码也能复述一次候选路径；图中实线 h=1、虚线 h=2/3；公式、图注、来源表和成本表的 h 含义一致，正文不声称计划扩展已完成。 |
| M2 | CLS 适配、候选残差评分与联合 SFT | 解释为何只适配 CLS、保留 256 个图像块、使用三轮交错融合与候选间比较，并给出 tanh 残差上界；随后用参数矩阵说明两项交叉熵的梯度边界。 | 未来误差可能破坏基础策略，因而需要可审计的残差幅度、候选范围和回退。SFT 的调整后损失若未停止上游梯度，会违反“只学习前视读取”的动机。 | 强方法段把结构选择与风险对应，而不是只列模块名。参数匹配对照和 zero-delta 性质是审稿人判断收益是否来自未来信息而非额外容量的标准模式。 | 期刊复现规范要求网络输入、维度、激活、初始化、掩码、损失、优化器顺序和可训练参数可追溯。STOP 应写成“分数不直接加残差”，不能夸成概率完全隔离。 | `native_cls_adapter.py` 支持五维条件与零初始化；`residual_head.py` 支持三轮交错融合、候选交互和 $delta_{max}\tanh$；`joint_e24.py` 支持 detached CPU fp16 replay、微批 2 和完整动作交叉熵。 | 将 E24 降为首次出现时的工程内部名，公开名称统一为候选残差评分头。增加 SFT 参数更新表，写明 owner/text/future/geometry/base score/mask/index/teacher 均停止梯度，两项损失在同一优化器 step 合并。 | PASS：停止分数与停止概率语义区分准确；adapter 与 patch 去向明确；调整后损失不会更新基础策略；zero-delta 和候选失败均能恢复基础行为。 |
| M3 | 冻结式 GRPO 与概率语义 | 说明 rollout 如何选前五候选、计算并缓存动作对齐残差、有效掩码和旧策略概率；更新阶段 current/reference 使用各自基础分数但复用同一缓存，旧概率直接读取缓存。 | “冻结参数”不等于“输出残差相同”。只有固定 residual vector、mask 和候选索引，策略比值与 KL 才不会混入动态 Top-K 切换，训练归因才符合控制性贡献。 | DeepSeekMath/PPO 提供组内优势与裁剪背景，ETP-R1 提供导航 GRPO 起点。审稿人会优先攻击 current/old/reference 输入不一致，因此实现契约必须比一般“模块冻结”更具体。 | 算法型期刊稿需声明行为策略、当前策略、参考策略、STOP 质量、强制停止过滤、可训练参数和恢复状态。尚未完成的新主动前视 GRPO 只能写方法和结果键，不能写进一步提升。 | `grpo_policy.py` 的 `frozen_lookahead_probs`、`GRPO_trainer_ETP_R1.py` 的 rollout buffer 与 update path、`frozen_grpo.py` 的冻结清单、`test_grpo_frozen_lookahead.py` 的 zero-delta 和三分布调用测试。 | 用公式 $pi^{LA}=\mathcal F(z_\theta,\bar\delta,m)$ 表示缓存语义；说明 GRPO 条件移动分布保持各自基础 STOP 概率质量，update_epochs=1 使样本只消费一次，唯一训练参数为基础导航策略。 | PASS：正文不再用冻结参数推导同一残差；current/reference 的 residual 与 mask 可逐元素比较；旧策略概率来源清楚；结果段仍保留 R2R/RxR GRPO 具名键。 |
| E1 | 数据集、同协议主结果与逐级消融 | 先定义 R2R-CE/RxR-CE 指标、比例尺度和协议键；主结果拆成 R2R 直接比较、RxR 直接比较、旧 ETP-R1 文献背景；逐级表声明每行唯一变化。 | 性能主张必须回答主动前视是否优于相应基座，但旧发布结果与现代视觉/控制器可能不可比。实验 1 还要区分视觉替换、RGB 融合、候选前视和强化预算。 | 高质量结果表把直接比较与 literature reported numbers 分面，并用零残差或无未来对照控制参数与训练预算。不能以同一验证集大量 checkpoint 中的幸运峰值代替稳定性。 | 期刊表格应自包含 dataset/split/language/controller/seed/CI，比例指标声明百分数，主表不使用过早粗体。多种子报告均值与场景聚类区间；单种子必须降级措辞。 | 已核验 R2R 基座 1,839 episode 的 SR/SPL；旧 ETP-R1 发布表通过 `ye2025etpr1` 引用。所有未知值由 `results_placeholder_manifest.md` 的 `DIRECT-*`、`R2R-*`、`RXR-*`、`A1-*` 键绑定原始 JSON。 | 主表只放现代同协议基座、主动前视 SFT、同预算零残差 GRPO 和冻结前视 GRPO；旧值独立列表且禁止求差。逐级消融为 R2R/RxR 分组，记录 Base→RAE→RGB→lookahead→GRPO。 | PASS：没有 val/test 混填、跨控制器差值或不统一尺度；每个单元格能追到清单；GRPO 增益同时对比同预算零残差而非只对比 SFT。 |
| E2 | 未来证据来源、候选级真实渲染与 h=1/2/3 | 区分参数匹配的无 q1、可部署 predicted q1 和固定头真实渲染诊断；再在预测分支比较第一、第二、第三步端点的导航、误差、覆盖、延迟和显存。 | 该单元的控制动机是回答收益受限于预测质量还是读取方式，并满足用户新增的向前 1/2/3 步对照。真实未来若只来自已执行分支会有选择偏差，因此必须覆盖所有前五候选。 | 参考文献与 SOTA 实验模式是固定一条因果链中的其他部分，只替换证据来源；但固定头输入替换有分布偏移，不能自动称性能上界。多步结果必须与误差累积和成本一起报告。 | 期刊复现规范要求 oracle 的模拟器状态、位姿、传感器、吸附阈值、碰撞、编码、覆盖、训练/评测用途明确。计划算法不能用完成时；h=2/3 需单独 config、smoke 和 manifest。 | 实现证据由 `dino_cwp_future.py` 保证 predicted 分支不查询未来环境；E10 给 h=1 两个完整诊断点；E12/E13 是用户确认的实验协议。`A2-*` 键绑定来源、视野、CLS/patch 误差、有效率和成本数据。 | 写作和实验改动为：无 q1 用零未来标记但保留候选掩码，单独同预算训练。真实渲染克隆/恢复 simulator state，以预测链固定 p1，所有前五候选同规则编码，不可达即无效并报告 coverage；h=2/3 各自独立训练。 | PASS：真实渲染不读取目标或教师动作，不用执行分支补齐；“CLS 误差为零”只指适配器前自身比较；h=2/3 在代码、测试和完整结果前始终标为计划扩展。 |
| E3 | 动作翻转、误差覆盖与性能--成本机制 | 把行为分析拆成相同初始 episode 的轨迹配对和相同记录状态的一步反事实；把预测质量、有效覆盖、基础分数间隔、候选预算、历史长度、视野和成本连接到导航结果。 | 当前翻转低于 0.3%，这是贡献显著性的核心风险。需要知道稀疏介入是否净修正、为何不介入，以及额外计算是否值得，而不是只报告 action flip rate。 | 强机制分析同时包含成功与失败、样本数与分母，并把相关性限制在给定设置。episode 首次分歧后的状态不可逐步严格配对，因此局部教师一致性与终局配对必须分开。 | 机器人期刊期待失败案例、停止错误、置信区间和部署成本。延迟必须固定 GPU、batch、预热和同步；表格列需给单位，图中不把相关关系画成因果箭头。 | E10 的 q0/q1 成功率、46.6%–49.6% 有效率和 0.17%–0.29% 翻转率；E14/E15 为用户确认的行为和机制实验。`A3-*` 键规定类别、全部 episode 分母、分歧分母和配对 SPL。 | episode 以首次分歧归类一次，状态层单列 STOP→MOVE 与 MOVE→STOP。误差使用适配器前 CLS 余弦距离和 256 patch 平均距离；候选覆盖按 Top-K 槽计算并分失败原因；开销逐模块计时。 | PASS：修正/伤害/效率/持续失败/无分歧五类分母清楚；不把单次动作写成终局因果；性能--成本曲线带硬件和区间；结论只采用真实数据支持的解释。 |
| D1 | 讨论、局限、结论与交付 | 讨论解释稀疏干预的期望净收益、缓存残差的归因优势和适用边界；结论只回收已验证系统主张与最终经验发现。中文 Word/PDF 作为实验回填前的逻辑母稿交付。 | 贡献意义取决于修正、伤害、覆盖和成本的共同证据。即使完整方法未提升，论文也应诚实说明预测、门控或基础策略的边界，不把负结果改写为正结果。 | 强讨论不枚举所有可能答案，而提出可证伪评价对象；强结论比摘要更克制。目标期刊未定时保留通用完整稿，待实验成熟后再决定 Transactions 长文或 Letters 精简稿。 | 期刊投稿前需清除所有结果键、作者占位和内部状态，套用当期模板，准备匿名/作者版本、预印本差异、AI 辅助披露和数据/代码可用性声明。当前只可称中文初稿完成。 | `reviewer_audit.md` 的 13 个异议、`humanize_report.md`、`integrity_audit.md`、两张 Graphviz 图、Word/PDF 渲染报告和 `final_artifact_manifest.md`。经验结论由 `CONCLUSION-EVIDENCE` 键约束。 | 讨论保留一个关于每次介入期望净收益的判断，并说明真实机器人、特定 RAE-NWM 和多步未完成三类限制。结论不重复全部模块细节，只留数据支持的发现和范围；交付文档保留具名键供用户替换。 | PASS：当前状态明确为非投稿就绪的中文初稿；正文无项目管理语句、无虚构数据、无文字方框图；Word/PDF 全页目视检查无裁切重叠。实验回填后必须重跑全部关卡。 |
